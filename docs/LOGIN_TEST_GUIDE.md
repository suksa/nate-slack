# ✅ 로그인 테스트 가이드

## 🎉 계정 준비 완료!

**이메일 확인 완료:** `suksa_@naver.com`

계정 정보:
- **이메일**: `suksa_@naver.com`
- **비밀번호**: `123123`
- **사용자 이름**: 조찬형
- **상태**: ✅ 이메일 확인됨, 프로필 생성됨

## 🚀 수동 테스트 방법

앱이 현재 실행 중입니다. 다음 단계를 따라 로그인을 테스트하세요:

### 1단계: 로그아웃 (이미 로그인된 경우)

현재 세션을 지우기 위해:
- 브라우저 개발자 도구 열기: `F12` 또는 `Ctrl+Shift+I`
- Console 탭에서 실행:
```javascript
localStorage.clear()
sessionStorage.clear()
location.reload()
```

### 2단계: 로그인

1. **로그인 탭 선택** (기본으로 선택되어 있어야 함)
2. **정보 입력:**
   - 이메일: `suksa_@naver.com`
   - 비밀번호: `123123`
3. **"로그인" 버튼 클릭**

### 3단계: 확인 사항

✅ **예상 결과:**
- "로그인 성공!" 메시지 (또는 즉시 워크스페이스 화면으로 이동)
- "Select Workspace" 페이지 표시
- 사용자 이름 "조찬형" 표시

❌ **오류가 발생하면:**
- 브라우저 콘솔(F12)에서 에러 메시지 확인
- 아래 "디버깅 명령어" 섹션 참조

## 🧪 자동 테스트 (Playwright)

터미널에서 실행:

```bash
# 로그인 테스트 실행
npx playwright test tests/login-test.spec.ts --headed

# 모든 테스트 실행
npx playwright test --headed
```

## 🔍 디버깅 명령어

브라우저 개발자 도구 콘솔(F12)에서:

### 현재 세션 확인
```javascript
// Supabase 세션 확인
const { data, error } = await supabase.auth.getSession()
console.log('Session:', data.session)
console.log('User:', data.session?.user)
```

### 로그인 테스트
```javascript
// 직접 로그인 시도
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'suksa_@naver.com',
  password: '123123'
})
console.log('Login result:', data)
console.log('Login error:', error)
```

### 프로필 확인
```javascript
// 현재 사용자 프로필 조회
const { data: user } = await supabase.auth.getUser()
if (user?.user) {
  const { data: profile } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.user.id)
    .single()
  console.log('Profile:', profile)
}
```

### 워크스페이스 확인
```javascript
// 현재 사용자의 워크스페이스 목록
const { data: user } = await supabase.auth.getUser()
if (user?.user) {
  const { data: workspaces } = await supabase
    .from('members')
    .select('workspace_id, workspaces(*)')
    .eq('user_id', user.user.id)
  console.log('Workspaces:', workspaces)
}
```

## 📊 데이터베이스 확인

현재 계정 상태:
```sql
-- auth.users 테이블
id: cff31e7f-4023-4061-b2ae-5a559e3660c1
email: suksa_@naver.com
email_confirmed_at: 2026-01-06 00:32:00 (✅ 확인됨)

-- profiles 테이블  
id: cff31e7f-4023-4061-b2ae-5a559e3660c1
username: 조찬형
full_name: 조찬형
```

## ✅ 성공 기준

로그인이 성공하면:
1. ✅ 오류 메시지 없음
2. ✅ "Select Workspace" 페이지로 이동
3. ✅ 사용자 정보 표시 (조찬형)
4. ✅ 워크스페이스 생성/선택 가능
5. ✅ 채널 접근 가능
6. ✅ 메시지 전송 가능

## 🎬 완전한 테스트 시나리오

1. **로그인** → 워크스페이스 선택 페이지
2. **워크스페이스 생성** (또는 기존 선택) → 메인 화면
3. **채널 생성** (또는 기존 선택) → 채팅 화면
4. **메시지 전송** → "안녕하세요! 테스트 메시지입니다 🚀"
5. **이모지 반응** → 메시지에 👍 추가
6. **스레드 답글** → 메시지에 답글 작성

모든 단계가 오류 없이 완료되면 **테스트 성공!** 🎉

## 📝 문제 발생 시

1. 터미널 로그 확인: `c:\Users\n4185\.cursor\projects\...\terminals\25.txt`
2. 브라우저 콘솔(F12) 오류 확인
3. 네트워크 탭에서 Supabase API 요청 확인
4. 위의 디버깅 명령어 실행

---

**현재 앱 실행 중:** `npm start` (포트 5174)

앱 창에서 로그인을 시도해보세요! 문제가 있으면 알려주세요.

